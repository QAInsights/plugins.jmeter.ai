{
  "permissions": {
    "allow": [
      "Shell(python3 -c import sys,json; d=json.load(sys.stdin); print(json.dumps({k:v for k,v in d.get('scripts',{}).items() if any(x in k for x in ['test','lint','check','type','vitest','tsc'])}, indent=2)))"
    ],
    "deny": [],
    "defaultMode": "default"
  }
}
